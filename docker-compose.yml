version: '3.8'

services:
  recolector:
    build: ./backend-recolector
    image: titulacion-local/recolector:latest
    container_name: recolector
    restart: always
    ports:
      - "8080:8080"
    volumes:
      - ./credenciales:/app/credenciales
    environment:
      - PORT=8080

  ml-python:
    build: ./backend-ml
    image: titulacion-local/ml-python:latest
    container_name: ml-python
    restart: always
    ports:
      - "8081:8080" 
    volumes:
      - ./modelo:/app/modelo
    environment:
      - PORT=8080

  verificador:
    build: ./backend-verificador
    image: titulacion-local/verificador:latest
    container_name: verificador
    restart: always
    ports:
      - "8082:8080" 
    volumes:
      - ./credenciales:/app/credenciales
    environment:
      - PORT=8080

  frontend:
    build: ./aplicacion_web_titulacion
    image: titulacion-local/frontend:latest
    container_name: frontend
    restart: always
    ports:
      - "80:80" 